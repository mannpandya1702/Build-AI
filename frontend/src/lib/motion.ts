/** Shared cubic-bezier easing (ease-out-expo). Typed as a 4-tuple so Framer
 *  Motion accepts it as a bezier definition rather than a loose number[]. */
export const EASE: [number, number, number, number] = [0.16, 1, 0.3, 1];
