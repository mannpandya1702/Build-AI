import { OG_SIZE, brandOgImage } from "@/lib/ogImage";

export const alt = "Destination weddings across India — Riwaaya";
export const size = OG_SIZE;
export const contentType = "image/png";

export default function Image() {
  return brandOgImage("Destination weddings across India", "Planned from Chandigarh, run on the ground");
}
